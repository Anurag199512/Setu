pipeline {
  agent: any

  stages {
    stage("build") {
      steps {
          echo "build complete"
      }
    }

    stage("deploy") {
      steps {
        echo "deploy complete"
      }
    }
  }
}
